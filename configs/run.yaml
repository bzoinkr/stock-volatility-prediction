project:
  name: stock-volatility-prediction
  timezone: America/New_York
  seed: 42

universe:
  tickers: ["AAPL"]
  start_date: "2025-01-01"
  end_date: "2026-02-06"

frequency:
  base: "1D"               # daily base timeline
  market_close_time: "16:00:00"

output:
  processed_dir: "data/processed"
  artifacts_dir: "artifacts"
  overwrite: false

keyword_count: 15          # number of keywords per ticker
peer_count: 6              # number of peer companies per base ticker
news_limit_per_ticker: 200 # max Yahoo news articles per ticker
